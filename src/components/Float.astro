<style>
    aside {
        float: right;
        clear: right;
        margin: 0 0 2rem 2.5rem;
        width: 200px;
        border: 1.5px solid var(--ink);
        background: var(--paper);
        overflow: hidden;
    }

    .aside-header {
        background: var(--ink);
        color: var(--paper);
        padding: 0.4rem 0.875rem;
        font-family: var(--font-mono);
        font-size: 0.6rem;
        letter-spacing: 0.14em;
        text-transform: uppercase;
    }

    .aside-body {
        padding: 0.875rem;
    }

    @media (max-width: 640px) {
        aside {
            float: none;
            width: 100%;
            margin: 0 0 2rem;
        }
    }
</style>

<aside>
    <div class="aside-header">Project Info</div>
    <div class="aside-body">
        <slot/>
    </div>
</aside>
