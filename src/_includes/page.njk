<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }} - {{ site.title }}</title>
    {% include "head.njk" %}
  </head>
  <body id="{{ page.fileSlug if page.fileSlug else 'index' }}">
    <div id="navigation-chrome" data-density-shift class="max-content">
      {% include "navigation.njk" %}
      <main id="main">
        {%- if img %}
        {% include "intro.njk" %}
        {% endif %}
        <div class="content">
          {{ content | safe }}
        </div>
        {%- if not page.fileSlug %}
        {% include "work.njk" %}
        {% endif %}
        <site-attribution></site-attribution>
      </main>
    </div>
  </body>
</html>